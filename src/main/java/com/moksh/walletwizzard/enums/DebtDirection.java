package com.moksh.walletwizzard.enums;

public enum DebtDirection {
    /** You lent money — creates a Receivable (ASSET) account. */
    LENT,
    /** You borrowed money — creates a Payable (LIABILITY) account. */
    BORROWED
}
