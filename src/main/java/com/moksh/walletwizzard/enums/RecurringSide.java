package com.moksh.walletwizzard.enums;

public enum RecurringSide {
    /** Outgoing — money leaves (subscription, expense, bill). */
    DEBIT,
    /** Incoming — money arrives (salary, dividend, rent received). */
    CREDIT
}
